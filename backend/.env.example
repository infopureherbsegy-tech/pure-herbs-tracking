# Local development can run without DATABASE_URL and uses backend/src/data.json.
# For permanent online persistence, set DATABASE_URL to your PostgreSQL connection string.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# Set DATABASE_SSL=false only for a local/non-SSL PostgreSQL server.
DATABASE_SSL=true
PORT=4000
